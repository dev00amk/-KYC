# The shipped scripts use only the Python standard library.
# Optional: serve dashboard/ with `python -m http.server`.
# Optional notebook exploration can use pandas/numpy, but CI and core scripts do not require them.
